export function getPublicLaunchMode(): {
  mode: "pilot";
  message: string;
} {
  return {
    mode: "pilot",
    message:
      "Birch Reserve is accepting qualified pilot, supply, and insight requests.",
  };
}